{
  "name": "cc-license-estimator",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:logic": "vitest run tests/logic.test.js",
    "test:security": "vitest run tests/security.test.js"
  },
  "devDependencies": {
    "jsdom": "^24.0.0",
    "vite": "^5.0.0",
    "vitest": "^2.0.0"
  }
}
